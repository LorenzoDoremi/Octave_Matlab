function a = table(row_names, col_names, values)

  cols = "";
  rows = "";
  for i= 1:size(col_names)

    cols = strcat(cols, {' '}, col_names(i));
  endfor
  char(cols(1))
  for i= 1:size(row_names)

  endfor


  end
